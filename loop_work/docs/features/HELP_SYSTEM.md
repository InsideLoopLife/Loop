# Help system

The help page lives at `/help` and is linked from the Account menu.

It accepts natural-language questions and answers from built-in feature context. When the saved OpenAI token is available, the `/api/help` route can use it to produce a better answer from the product documentation context.
